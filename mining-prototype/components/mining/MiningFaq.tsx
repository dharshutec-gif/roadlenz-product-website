"use client";import{useState}from"react";import{AnimatePresence,motion,useReducedMotion}from"framer-motion";import{mining as d}from"@/data/mining";export function MiningFaq(){const r=useReducedMotion(),[open,setOpen]=useState<number|null>(0);return <section className="section faq"><div className="shell faq-inner"><h2>Frequently asked questions</h2><div>{d.faqs.map((x,i)=>{const a=i===open;return <article key={x.q}><button aria-expanded={a} onClick={()=>setOpen(a?null:i)}>{x.q}<motion.i animate={{rotate:a?45:0}}>+</motion.i></button><AnimatePresence initial={false}>{a&&<motion.p initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} transition={{duration:r?0:.25}}>{x.a}</motion.p>}</AnimatePresence></article>})}</div></div></section>}
