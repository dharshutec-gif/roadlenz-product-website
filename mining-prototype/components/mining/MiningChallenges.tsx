"use client";import{motion,useReducedMotion}from"framer-motion";import{mining as d}from"@/data/mining";export function MiningChallenges(){const r=useReducedMotion();return <section className="section challenges"><div className="shell"><h2>Built for demanding sites.</h2><p className="lead">Mining and extractive operations face unique visibility and safety challenges.</p><motion.div className="challenge-grid" initial="hide" whileInView="show" viewport={{once:true}} variants={{show:{transition:{staggerChildren:r?0:.12}}}}>{d.challenges.map((x,i)=><motion.article key={x.title} variants={{hide:{opacity:0,y:r?0:14},show:{opacity:1,y:0}}}><span>{i===0?"◉":i===1?"△":"▣"}</span><h3>{x.title}</h3><p>{x.text}</p></motion.article>)}</motion.div></div></section>}
