"use client";import{motion,useReducedMotion}from"framer-motion";import{mining as d}from"@/data/mining";export function MiningBenefits(){const r=useReducedMotion();return <section className="section benefits"><motion.div className="shell benefits-grid" initial="hide" whileInView="show" viewport={{once:true}} variants={{show:{transition:{staggerChildren:r?0:.1}}}}>{d.benefits.map((x,i)=><motion.article variants={{hide:{opacity:0,y:r?0:12},show:{opacity:1,y:0}}} key={x.title}><span>{i===0?"♢":i===1?"⚙":i===2?"◉":"▤"}</span><h3>{x.title}</h3><p>{x.text}</p></motion.article>)}</motion.div></section>}
