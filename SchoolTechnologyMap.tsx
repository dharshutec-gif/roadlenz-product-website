"use client";
import Image from "next/image";
import { motion,useReducedMotion } from "framer-motion";
import { schoolTransport as data } from "@/data/schoolTransport";
export function SchoolTechnologyMap(){const r=useReducedMotion();return <section id="technology" className="technology section"><div className="shell tech-layout"><div className="tech-copy"><h2>One school journey.<br/>One connected view.</h2><i/><p>RoadLenz devices work together to capture, connect, and simplify every movement on the road—from pickup to drop-off.</p></div><div className="tech-stage"><div className="connector-field" aria-hidden><motion.span animate={r?{opacity:0}:{offsetDistance:["0%","100%"],opacity:[0,1,1,0]}} transition={r?{duration:0}:{duration:4,repeat:Infinity,ease:"linear"}}/></div><Image className="bus-cutout" src="/images/school-bus-cutout.png" alt="School bus with connected RoadLenz technology" width={660} height={380}/>{data.devices.map((d,i)=><motion.article className={`device-callout callout-${i+1}`} key={d.name} initial={{opacity:0,y:r?0:16}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:r?0:i*.1}}><motion.div animate={r?{y:0}:{y:[0,-6,0]}} transition={r?{duration:0}:{duration:5+i*.25,repeat:Infinity,ease:"easeInOut"}}><Image src={d.image} alt="" width={82} height={64}/></motion.div><h3>{d.name}</h3><p>{d.benefit}</p></motion.article>)}</div></div></section>}
