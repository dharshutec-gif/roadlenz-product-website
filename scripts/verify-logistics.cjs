const fs=require('fs'),ts=require('typescript'),assert=require('node:assert/strict');const m={exports:{}};new Function('exports','require','module',ts.transpileModule(fs.readFileSync('src/lib/logistics-content.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText)(m.exports,require,m);const upgrade=m.exports.upgradeLogisticsSolution;const db=JSON.parse(fs.readFileSync('data/db.json','utf8'));const source=db.solutions.find(s=>s.slug==='logistics');const copy=structuredClone(source);upgrade(copy);const once=JSON.stringify(copy);upgrade(copy);assert.equal(JSON.stringify(copy),once);const custom={...structuredClone(source),painPoints:[],benefits:[],faqs:[],journeyStages:[],recommendedProducts:[],liveStatus:[],logisticsContent:{challengeTitle:'Custom heading',mediaFallback:''}};upgrade(custom);for(const key of ['painPoints','benefits','faqs','journeyStages','recommendedProducts','liveStatus'])assert.deepEqual(custom[key],[]);assert.equal(custom.logisticsContent.challengeTitle,'Custom heading');assert.equal(custom.logisticsContent.mediaFallback,'');assert.deepEqual(copy.relatedProductSlugs,source.relatedProductSlugs);assert.deepEqual(copy.recommendedProducts,source.recommendedProducts);const other={slug:'taxi',name:'Untouched'};upgrade(other);assert.deepEqual(other,{slug:'taxi',name:'Untouched'});console.log('PASS migration idempotence, custom/empty preservation, product assignments, non-Logistics guard');
