const {chromium}=require('C:/Users/acer/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});try{for(const mode of ['no-preference','reduce']){const p=await b.newPage({viewport:{width:1440,height:1000},reducedMotion:mode});const errors=[];p.on('console',m=>{if(m.type()==='error'&&/hydrat/i.test(m.text()))errors.push(m.text())});p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost:3000/industries',{waitUntil:'networkidle'});const explorer=p.locator('#industry-explorer');await explorer.scrollIntoViewIfNeeded();await p.mouse.move(0,0);const cutout=explorer.locator('[class*="cutout"]').first();const first=await cutout.evaluate(e=>getComputedStyle(e).scale);await p.waitForTimeout(1100);const second=await cutout.evaluate(e=>getComputedStyle(e).scale);if(mode==='reduce')assert.equal(first,second);else assert.notEqual(first,second);const advantage=p.locator('section[aria-labelledby="roadlenz-intelligence-title"]');await advantage.scrollIntoViewIfNeeded();await p.waitForTimeout(1100);const layer=advantage.locator('[data-layer]');const a=await layer.evaluate(e=>getComputedStyle(e).transform);await p.waitForTimeout(900);const z=await layer.evaluate(e=>getComputedStyle(e).transform);if(mode==='reduce')assert.equal(a,z);else assert.notEqual(a,z);assert.deepEqual(errors,[]);assert.equal(await explorer.evaluate(e=>getComputedStyle(e).opacity),'1');await p.emulateMedia({reducedMotion:mode==='reduce'?'no-preference':'reduce'});await p.waitForTimeout(250);assert.deepEqual(errors,[]);console.log('PASS',mode,'hydration, visible explorer, vehicle and Advantage motion, preference change');await p.close();}}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
