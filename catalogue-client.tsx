"use client";
import { useEffect, useState } from "react";
export async function catalogueApi<T>(path:string,method="GET",body?:unknown):Promise<T>{const res=await fetch(`/api/admin/catalogue/${path}`,{method,headers:body?{"Content-Type":"application/json"}:undefined,body:body?JSON.stringify(body):undefined,cache:"no-store"});const data=await res.json();if(!res.ok)throw new Error(data.error||"Request failed.");if(method!=="GET")window.dispatchEvent(new Event("roadlenz:admin-refresh"));return data as T;}
export function useCatalogue<T>(path:string){const[data,setData]=useState<T|null>(null);const[error,setError]=useState("");const[loading,setLoading]=useState(true);function reload(){setLoading(true);catalogueApi<T>(path).then(setData).catch(e=>setError(String(e.message))).finally(()=>setLoading(false));}useEffect(()=>{reload();},[path]);return{data,error,setError,loading,reload};}
export async function uploadAsset(file:File,category:string):Promise<string>{const form=new FormData();form.append("file",file);form.append("category",category);const res=await fetch("/api/upload",{method:"POST",body:form});const body=await res.json();if(!res.ok)throw new Error(body.error||"Upload failed.");window.dispatchEvent(new Event("roadlenz:admin-refresh"));return body.url;}
